All the functions are as per the convention. 
Just the set and get burst time are differently named.
setBurstTime and getBurstTime.

We have implemented a general case.
Currently it is running shortest job scheduling first case.
To shift to hybrid scheduling uncomment the second last code segment in the trap.h
